# Summer Cut

Personal passcode-locked fitness tracker PWA. Vanilla HTML/CSS/JS, zero dependencies, no backend — all user data stays in the browser's localStorage. Content is AES-256-GCM encrypted at rest; the passcode never leaves the device.

Static hosting via GitHub Pages.
